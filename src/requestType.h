#ifndef _CROSSAVER_H_REQUEST_TYPE
#define _CROSSAVER_H_REQUEST_TYPE

typedef enum RequestType {
    SCREENSAVER,
    PREVIEW,
    DEBUG,
    CONFIG
} RequestType;

#endif // _CROSSAVER_H_REQUEST_TYPE
